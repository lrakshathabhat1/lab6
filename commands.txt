git config --global --replace-all user.name KshamaV24
git config --global --replace-all user.email kshamabv24@gmail.com

git init

git add 1.c
git commit -m "1 committed"

git checkout -b feature

git add 2.c
git commit -m "2 committed"

git add 3.c
git commit -m "3 committed"

git add 4.c
git commit -m "4 committed"

git switch master

git add 5.c
git commit -m "5 committed"

git merge feature -m "custom commit message for 6th experiment"
git log --all --graph
